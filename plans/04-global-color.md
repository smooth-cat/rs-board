# 持久化全局颜色

1. 用户每次手动选择颜色，都将全局颜色设置为该颜色并持久化保存
2. 当用户选择了一个与当前颜色不同的元素时，仅界面上将展示为对应颜色，不对全局颜色做持久化保存
3. 当用户取消选择时，切换回全局颜色

兼容约束：设置中新增 `global_color` 字段，旧的 `ToolDefaultStyle.color_rgba` 字段保留但不再作为运行时颜色来源；旧设置缺少新字段时按红色默认值读取。
